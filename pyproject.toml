[project]
name = "privacy-toolkit"
version = "1.0.0"
description = "Pseudonymize, anonymize, then attack synthetic personal data."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"
