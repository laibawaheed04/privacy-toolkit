# Synthetic data only

`raw_data.csv` is produced by `src/generate_data.py` (Faker, seed=42).
It is not real personal data.

`pseudonymized.csv`, `anonymized.csv`, and `voter_list.csv` are derived
releases. The identifier vault lives in `secrets/` and is git-ignored.
